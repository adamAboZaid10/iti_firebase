import 'package:flutter/material.dart';
class HommPage extends StatelessWidget {
  const HommPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("HomePage"),
      ),
    );
  }
}
